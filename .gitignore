# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
.env.example
dist/
build/

# Virtual environments
.venv/
venv/
env/
.env
# Jupyter
.ipynb_checkpoints/

# IDE
.vscode/
.idea/

# Testing
.pytest_cache/

# OS
.DS_Store
Thumbs.db

# Claude Code
.claude/

# Project artifacts (not part of deliverable)
checkpoint.json
cloud_checkpoint.json
week4_results.json
CLAUDE.md
next_steps_cloud.md

# Root-level duplicate (canonical copy is in notebooks/)
/week4_implementation.ipynb

# Folders not part of deliverable
exploration/
cloud_results/
pod_results/
prompts_IGNORE/

# RunPod secrets and session state
.env
gpupod/.session.json
cpupod/.session*.json

# Private keys (never commit)
*.pem
*.key

# Large data files (exceed GitHub 100MB limit)
data/*.npy
data/*.dat
data/_shards_L2/
data/**/_shards_L2/
data/subtree_test/

# Enormous shard/checkpoint trees (multi-GB; exceed GitHub 2GB hard blob limit)
*.npy
*.dat
*.tar
*.tar.gz
*.tgz
archive/data_runs/
cloninger-steinerberger/cpu/data/
/data/
/data_experiment/
**/_shards_L1/
**/_shards_L2/
**/coarse_shards/
**/checkpoints/
**/subtree_test/

# Root-level cruft (one-off scripts, orphan artifacts)
/.fix_indent_damage.py
/lower_bound_proof.log

# Temp/backup files
*.stackdump
*.bak

# LaTeX build artefacts (keep .tex / .pdf / .bib / figures only)
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.lof
*.lot
*.out
*.synctex.gz
*.toc
delsarte_dual/**/*.log
delsarte_dual/**/texput.log

# OneDrive sync race quarantine — review then delete
_resurrected_quarantine/
CLAUDE.md